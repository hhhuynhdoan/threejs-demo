
## Frontend
- Install package: `npm install`
- Start: `npm run start`


**Way 2**
  `bash build.bash dev` for dev enviroment
  `bash build.bash test` for test enviroment
  `bash build.bash staging` for staging enviroment
  `bash build.bash prod` for production enviroment
  